#include "main.h"

/**
  * add - adds two integers
  * @a: first operand
  * @b: second operand
  *
  * Return: returns the result of adding two integers
  */
int add(int a, int b)
{
	 int a, b, sum;

	 sum = a + b;
	 return (sum);
}

