#include "main.h"

/**
  * print_rev - prints a string, in reverse, followed by a new line
  * @s: pointer to string to print
  *
  * Return: returns void
  */
lf(char *str)
{
	int i;
	int n, len;
	n = 0;

	
	while (*s++)
		len++;
	if (len % 2 = 0)
		n = len / 2;
	else
		n = (len - 1) / 2;
	
	for (i = 0; i < n; i ++)
		_putchar(*(str++));
	_putchar('\n');
}
